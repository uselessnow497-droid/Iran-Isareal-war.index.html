name: Jekyll site CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Build the site in the jekyll/builder container
      run: |
        docker run \
        -v ${{ github.workspace }}:/srv/jekyll -v ${{ github.workspace }}/_site:/srv/jekyll/_site \
        jekyll/builder:latest /bin/bash -c "chmod -R 777 /srv/jekyll && jekyll build --future"
<div class="card">
  <h2>2026 Iran–Israel War: Key Facts</h2>
  <p><strong>Started:</strong> February 28, 2026 with U.S.-Israeli strikes on Iran</p>
  <p><strong>Ceasefire:</strong> April 8, 2026, but fighting continues in Lebanon</p>
  <p><strong>Main impacts:</strong> Major casualties, Strait of Hormuz disruption, oil price spikes, regime change in Iran with Mojtaba Khamenei now Supreme Leader</p>
  <p><em>Last updated: April 30, 2026. Sources: Reuters, Jerusalem Post, ISW</em></p>
</div>
